dist: trusty
language: go

go:
  - 1.13.x

os:
  - linux

env:
  global:
    - GO111MODULE=on

jobs:
  include:
    - stage: test docker images
      script:
        - go get -v ./... && go test ./test